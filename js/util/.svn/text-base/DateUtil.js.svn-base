/**
 * Created by xtdhwl on 10/15/16.
 */


var DateUtil = {


    formatString: function (dateStr) {
        //TODO 未实现
        return null;
    },

    formatDate: function (dateLong) {
        var date = new Date();
        date.setMilliseconds(dateLong);
        var Y = date.getFullYear() + '-';
        var M = (date.getMonth()+1 < 10 ? '0'+(date.getMonth()+1) : date.getMonth()+1) + '-';
        var D = date.getDate() + ' ';
        var h = date.getHours() + ':';
        var m = date.getMinutes() + ':';
        var s = date.getSeconds();
        return Y+M+D+h+m+s;
    }

};